import "./cta.css";

const CTA = () => {
  return (
    <section className="cta" id="cta">
      <div className="cta-container">
        <h2>Ready to grow faster?</h2>
        <p>
          Join thousands of teams using Sammunat to manage work efficiently.
        </p>
        <button className="cta-btn">Get Started Free</button>
      </div>
    </section>
  );
};

export default CTA;
